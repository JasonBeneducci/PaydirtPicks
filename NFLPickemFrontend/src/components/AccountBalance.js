import React from 'react'

const AccountBalance = () => {
    // Hardcoded account balance for every player for now, but once users are persisted this will be a user attribute
        return (
            <div>
                Your balance is currently $100. Each entry to play will cost you $1.
            </div>
        )
}
export default AccountBalance